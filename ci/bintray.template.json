{
    "package": {
        "name": "nightly",
        "repo": "FSO",
        "subject": "asarium"
    },

    "version": {
        "name": "$VERSION_NAME"
    },

    "files": [
        {
            "includePattern": "/tmp/builds/(.*)",
            "uploadPattern": "$1"
        }
    ],
    "publish": true
}