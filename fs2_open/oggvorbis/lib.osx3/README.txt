This directory contians special ogg libs for OSX compiled with GCC 3 with
an extra ranlib run on them for updating purposes.  Take note of the "_fs"
name extension which is there to get around an Xcode linking issue that
would prefer dylibs over these static versions.  
