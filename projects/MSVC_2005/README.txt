If you're going to use this project, you need to rename fs2_open/lua/lua.cpp to lua2.cpp. It's the only way it'll work.

Only Debug configuration currently works. Still trying to get Release config up.